Ảnh: lab-map.PNG — CONFIG.labMap.imageUrl trùng tên file.

contentInset: { left, top, right, bottom } — % cắt viền trắng trên file PNG (vùng % sau đó tính trên phần còn lại).

zones: value, label, hint, rect — rect là % trên vùng sau contentInset.
